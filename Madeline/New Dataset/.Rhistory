max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
# Plot
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14) +
labs(
title = paste("Growth Chart for", input$domain_select),
x = "Age (months)",
y = "Transformed Bayley Score",
color = "Percentile"
)
})
}
output$growth_plot <- renderPlot({
req(input$domain_select, input$sct_select)
# Filter data safely
plot_data <- new_gsv_long_rem %>%
filter(!is.na(domain), !is.na(sca_condition), !is.na(bsid_age_calc), !is.na(transformed_score)) %>%
filter(domain == input$domain_select)
if (input$sct_select != "ALL") {
plot_data <- plot_data %>% filter(sca_condition == input$sct_select)
}
validate(
need(nrow(plot_data) > 10, "Not enough data after filtering.")
)
# Fit GAMLSS model
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
# Plot
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14) +
labs(
title = paste("Growth Chart for", input$domain_select),
x = "Age (months)",
y = "Transformed Bayley Score",
color = "Percentile"
)
})
plot_data <- new_gsv_long_rem %>%
filter(!is.na(domain), !is.na(sca_condition), !is.na(bsid_age_calc), !is.na(transformed_score)) %>%
filter(domain == input$domain_select)
plot_data <- new_gsv_long_rem %>%
filter(!is.na(domain), !is.na(sca_condition), !is.na(bsid_age_calc), !is.na(transformed_score))
# UI
ui <- fluidPage(
titlePanel("Growth Charts (Bayley Scores)"),
sidebarLayout(
sidebarPanel(
selectInput("domain_select", "Select Domain:",
choices = unique(na.omit(new_gsv_long_rem$domain))),
selectInput("sct_select", "Select SCT Condition:",
choices = c("ALL", unique(na.omit(new_gsv_long_rem$sca_condition))),
selected = "ALL")
),
mainPanel(
plotOutput("growth_plot"),
br(),
p("Figure caption: see explanation in Tab 5")
)
)
)
# ---- Server logic ----
server <- function(input, output, session) {
output$growth_plot <- renderPlot({
req(input$domain_select, input$sct_select)
# Filter data safely
plot_data <- new_gsv_long_rem %>%
filter(domain == input$domain_select)
if (input$sct_select != "ALL") {
plot_data <- plot_data %>% filter(sca_condition == input$sct_select)
}
validate(
need(nrow(plot_data) > 10, "Not enough data after filtering.")
)
# Fit GAMLSS model
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
# Plot
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14) +
labs(
title = paste("Growth Chart for", input$domain_select),
x = "Age (months)",
y = "Transformed Bayley Score",
color = "Percentile"
)
})
}
# Run the app
shinyApp(ui = ui, server = server)
# Load libraries
install.packages("gamlss.add")
library(shiny)
library(gamlss)
library(gamlss.add)
library(dplyr)
library(tidyr)
library(ggplot2)
library(tidyverse)
install.packages("gamlss.add")
new_gsv_long_rem <- readRDS("new_gsv_long_rem.rds")
selectInput("domain_select", "Select Domain:",
choices = unique(na.omit(new_gsv_long_rem$domain)))
selectInput("sct_select", "Select SCT Condition:",
choices = c("ALL", unique(na.omit(new_gsv_long_rem$sca_condition))),
selected = "ALL")
renderPlot({
req(input$domain_select, input$sct_select)
plot_data <- new_gsv_long_rem %>%
filter(!is.na(domain), !is.na(sca_condition), !is.na(bsid_age_calc), !is.na(transformed_score)) %>%
filter(domain == input$domain_select)
if (input$sct_select != "ALL") {
plot_data <- plot_data %>% filter(sca_condition == input$sct_select)
}
validate(
need(nrow(plot_data) > 10, "Not enough data after filtering.")
)
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14) +
labs(
title = paste("Growth Chart for", input$domain_select),
x = "Age (months)",
y = "Transformed Bayley Score",
color = "Percentile"
)
})
# Run the application
shinyApp(ui = ui, server = server)
# Load libraries
library(shiny)
library(gamlss)
library(gamlss.add)
library(dplyr)
library(tidyr)
library(ggplot2)
library(tidyverse)
new_gsv_long_rem <- readRDS("new_gsv_long_rem.rds")
selectInput("domain_select", "Select Domain:",
choices = unique(na.omit(new_gsv_long_rem$domain)))
selectInput("sct_select", "Select SCT Condition:",
choices = c("ALL", unique(na.omit(new_gsv_long_rem$sca_condition))),
selected = "ALL")
renderPlot({
req(input$domain_select, input$sct_select)
plot_data <- new_gsv_long_rem %>%
filter(!is.na(domain), !is.na(sca_condition), !is.na(bsid_age_calc), !is.na(transformed_score)) %>%
filter(domain == input$domain_select)
if (input$sct_select != "ALL") {
plot_data <- plot_data %>% filter(sca_condition == input$sct_select)
}
validate(
need(nrow(plot_data) > 10, "Not enough data after filtering.")
)
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14) +
labs(
title = paste("Growth Chart for", input$domain_select),
x = "Age (months)",
y = "Transformed Bayley Score",
color = "Percentile"
)
})
# ---- Load your dataset ----
# You must load your dataset BEFORE server starts
# Change this path as needed if the CSV is external
new_gsv_long_rem <- readRDS("new_gsv_long_rem.rds")
runApp()
runApp()
View(new_gsv_long_rem)
unique(na.omit(new_gsv_long_rem$domain)))
unique(na.omit(new_gsv_long_rem$domain))
plot_data <- new_gsv_long_rem %>%
filter(domain == "Cognitive")
runApp()
runApp()
runApp()
runApp()
runApp()
View(plot_data)
runApp()
plot_data <- new_gsv_long_rem %>%
filter(domain == "Expressive") %>%
dplyr::select(study_id_extraordinary, domain, bsid_age_calc, transformed_score) %>%
filter(complete.cases(.))
uniqeu(new_gsv_long_rem$domain)
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
age_seq
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 50)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 25)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
# Plot
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14) +
labs(
title = paste("Growth Chart for", input$domain_select),
x = "Age (months)",
y = "Transformed Bayley Score",
color = "Percentile"
)
runApp()
plot_data <- new_gsv_long_rem %>%
filter(domain == "Receptive") %>%
dplyr::select(study_id_extraordinary, domain, bsid_age_calc, transformed_score) %>%
filter(complete.cases(.))
# Fit GAMLSS model
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
View(pred_long)
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
)
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
plot_data <- new_gsv_long_rem %>%
filter(domain == "Cognitive") %>%
dplyr::select(study_id_extraordinary, sca_condition, domain, bsid_age_calc, transformed_score)
runApp()
plot_data <- new_gsv_long_rem %>%
filter(domain == "Cognitive") %>%
dplyr::select(study_id_extraordinary, sca_condition, domain, bsid_age_calc, transformed_score)
View(plot_data)
runApp()
plot_data <- new_gsv_long_rem %>%
filter(domain == "Cognitive") %>%
dplyr::select(study_id_extraordinary, sca_condition, domain, bsid_age_calc, transformed_score)
View(plot_data)
plot_data <- na.omit(plot_data)
validate(
need(nrow(plot_data) > 10, "Not enough data after filtering.")
)
# Fit GAMLSS model
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
runApp()
runApp()
# ---- Load your dataset ----
# You must load your dataset BEFORE server starts
# Change this path as needed if the CSV is external
new_gsv_long_rem <- readRDS("new_gsv_long_rem.rds") %>%
mutate(sca_condition = as.character(sca_condition))
plot_data <- plot_data %>% filter(sca_condition == "XXY")
plot_data <- na.omit(plot_data)
validate(
need(nrow(plot_data) > 10, "Not enough data after filtering.")
)
# Fit GAMLSS model
model <- gamlss(
formula = transformed_score ~ pb(bsid_age_calc, lambda = 5),
sigma.formula = ~pb(bsid_age_calc),
nu.formula = ~1,
tau.formula = ~1,
data = plot_data,
family = BCCG()
)
# Predict centiles
age_seq <- seq(min(plot_data$bsid_age_calc, na.rm = TRUE),
max(plot_data$bsid_age_calc, na.rm = TRUE),
length.out = 100)
qvals <- centiles.pred(model, xname = "bsid_age_calc", xvalues = age_seq,
cent = c(10, 25, 50, 75, 90), plot = FALSE)
pred_long <- pivot_longer(as.data.frame(qvals), -x,
names_to = "Percentile", values_to = "Score")
# Plot
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14) +
labs(
title = paste("Growth Chart for", input$domain_select),
x = "Age (months)",
y = "Transformed Bayley Score",
color = "Percentile"
)
ggplot(pred_long, aes(x = x, y = Score, color = Percentile)) +
geom_line(size = 1.2) +
geom_ribbon(
data = data.frame(
x = qvals$x,
ymin = qvals$`10`,
ymax = qvals$`90`
),
aes(x = x, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
alpha = 0.2,
fill = "gray70"
) +
theme_minimal(base_size = 14)
runApp()
runApp()
View(plot_data)
runApp()
runApp()
runApp()
?gamlss
runApp()
runApp()
runApp()
runApp()
